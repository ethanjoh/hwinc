# 변경 이력 (Changelog)

이 프로젝트의 업데이트 및 최적화 작업 내역을 날짜별로 정리한 변경 이력 파일입니다.

---

## [2026-06-29]

### 성능 개선 (Perf)
- **메인 페이지 동영상 로딩 시 검은 화면 문제 해결 (`index.php` / `custom.css`)**
  - **문제 현상**: 메인 페이지 진입 시 Revolution Slider 배경 동영상(`home_movie5.mp4`)이 로드되기 전까지 슬라이더 영역이 검은 화면으로 표시되던 문제를 수정했습니다.
  - **Poster 이미지 추가**: 배경 동영상의 1초 지점 프레임을 `ffmpeg`로 캡처하여 포스터 이미지(`home_movie5_poster.jpg`, **126 KB, 1280×720**)를 생성하고, `rev-slidebg` 클래스로 슬라이더에 적용했습니다. 동영상 로드가 완료되면 Revolution Slider가 자동으로 영상으로 전환합니다.
  - **슬라이더 배경색 안전장치**: 포스터 이미지조차 로드되지 않은 극단적 상황을 대비하여 슬라이더 컨테이너에 어두운 배경색(`#1a1a2e`)을 `custom.css`에 추가했습니다.
  - **체감 로딩 순서**: 배경색(`#1a1a2e`) → poster 이미지 → 동영상 자동 전환

- **메인 페이지 동영상 로딩 지연 추가 개선 (`index.php` / `css/custom.css` / `include/header.php`)**
  - **문제 현상**: Revolution Slider 초기화 시 `data-videopreload="auto"` 설정으로 인해 동영상 전체가 버퍼링될 때까지 로딩 스피너(`.tp-loader`)가 표시되어 사용자 체감 속도가 느린 문제를 수정했습니다.
  - **videopreload 전략 변경** (`index.php`): `data-videopreload="auto"` → `"metadata"` 로 변경하여 동영상 전체를 미리 내려받지 않고 메타데이터만 로드한 뒤 즉시 재생을 시작하도록 개선했습니다.
  - **포스터 이미지 preload 추가** (`include/header.php`): `<link rel="preload" as="image">` 태그를 `<head>` 내에 삽입하여 브라우저가 HTML 파싱 초기부터 포스터 이미지를 최우선 요청하도록 했습니다.
  - **CSS 배경 즉시 표시** (`css/custom.css`): `.rev_slider_wrapper`에 포스터 이미지를 CSS 배경으로 직접 설정하고, `.tp-loader`를 투명 처리하여 JS 초기화 전에도 빈 화면 없이 포스터가 즉시 표시되도록 했습니다.

---

### UI/UX 개선 및 마크업 정리 (Refactor)
- **관리자 페이지 네비게이션 메뉴 속성 및 마크업 개선 (`admin/include/navigation.php`)**
  - **지도 그리기 링크 동작 변경**: 기존 새 창(`target="_blank"`)으로 열리던 '지도 그리기' 메뉴를 현재 창에서 바로 열리도록 수정하여, 관리자 페이지 내 이동의 일관성을 확보했습니다.
  - **마크업 포맷팅 정돈**: 대시보드 메뉴의 아이콘과 텍스트(`<h4>`) 태그 사이에 줄바꿈을 적용해 코드 가독성을 높이고, 상단 사용자 아이콘 영역의 불필요한 공백을 제거했습니다.
- **관리자 대시보드 판넬 디자인 변경 (`admin/main.php`)**
  - **직관성 개선**: '지도 그리기' 메뉴 판넬의 테마 색상을 기존 파란색(`panel-primary`)에서 노란색(`panel-yellow`)으로 변경하여 다른 판넬들과 시각적으로 명확히 구분되도록 개선했습니다.

---

### 버그 수정 (Fix)
- **브라우저 콘솔 에러 2건 해결 (`include/header.php` / `include/script.php`)**
  - **에러 1 — `sdcomm.co.kr` 접속 불가** (`ERR_CONNECTION_REFUSED`): `<head>` 내에 삽입되어 있던 SDCOMM 로그 분석 스크립트(`//sdcomm.co.kr/ip_trace/bot.js`)가 서버 종료로 연결 자체가 거부되어 렌더링 블로킹을 유발하던 문제를 수정했습니다. 해당 스크립트 블록을 `include/header.php`에서 제거했습니다.
  - **에러 2 — 네이버 광고 추적 모듈 차단** (`ERR_BLOCKED_BY_CLIENT`): 네이버 웹 로그 분석 스크립트(`wcslog.js`)가 내부적으로 로드하는 광고 추적 모듈을 사용자 광고차단기가 차단하면서 콘솔 에러로 노출되던 문제를 수정했습니다. `include/script.php`의 wcs 호출 코드를 `try-catch`로 감싸 에러 전파를 방지했습니다. (광고차단기 미사용 일반 방문자에게는 기존과 동일하게 동작합니다.)
- **관리자 페이지 카카오맵 인쇄 시 화면 잘림 현상 수정 (`admin/draw-map.php`)**
  - **문제 현상**: '지도 인쇄하기' 버튼 클릭 또는 브라우저 인쇄(Ctrl+P) 시 카카오맵의 우측과 하단 영역이 잘리거나 하얗게 여백으로 출력되는 문제를 수정했습니다.
  - **해결 방법**: 
    - 인쇄 모드 진입 전후로 화면 크기가 변경되어 타일 로딩이 누락되지 않도록 인쇄 전 화면(`.printing-mode`)과 실제 인쇄 화면(`@media print`)의 지도 컨테이너 크기를 A4 가로 인쇄 영역에 맞추어 동일하게 고정(`width: 277mm; height: 175mm;`)했습니다.
    - 브라우저의 `beforeprint` 이벤트에서 중복으로 발생하는 지도 크기 재계산(`relayout`) 로직을 제어하기 위해 `isPrintingAction` 상태값을 추가했습니다.
    - 지도가 변경된 해상도에 맞춰 모든 타일 이미지를 가져올 수 있도록 인쇄 다이얼로그 호출 대기 시간을 600ms에서 800ms로 늘렸습니다.
- **데이터베이스 저장 로직 보안 취약점 및 오류 수정 (`admin/save-billing.php`)**
  - **문제 현상**: 폼에서 전달받은 데이터를 검증 없이 SQL 쿼리에 삽입하여, 입력값에 따옴표(`'`)가 포함될 경우 문법 오류가 발생하거나 SQL 인젝션 공격에 취약했습니다. 또한 일부 필드 누락 시 PHP 경고(Warning)가 발생하고 입력 데이터의 공백이 제대로 제거되지 않는 문제도 발견되었습니다.
  - **해결 방법**:
    - `mysqli_real_escape_string()`을 모든 입력값에 적용하여 쿼리 오류 방지 및 SQL 인젝션 공격을 차단했습니다.
    - 널 병합 연산자(`?? ''`)를 사용하여 데이터 누락 시 빈 문자열로 안전하게 처리해 PHP 에러를 방지했습니다.
    - 모든 데이터 변수에 `trim()`을 적용하여 불필요한 공백을 제거하도록 통일했습니다.

---


### 버전 관리 및 보안 강화 (Chore & Security)
- **FTP 접속 정보 파일(`sftp.json`) Git 히스토리 완전 제거**
  - VS Code SFTP 확장 설정 파일(`.vscode/sftp.json`)에 FTP 비밀번호가 평문으로 포함된 채 GitHub에 커밋되어 있던 문제를 발견하여 조치했습니다.
  - `git filter-repo`를 사용하여 **전체 커밋 히스토리(37개 커밋)에서 해당 파일을 완전히 제거**하고, force push로 원격 저장소에 반영했습니다.
  - `.vscode/` 디렉토리 전체를 `.gitignore`에 등록하여 향후 IDE 설정 파일이 재커밋되지 않도록 방지했습니다.
- **데이터베이스 덤프 파일(`billing.sql`) Git 추적 제거**
  - 로컬 데이터베이스 덤프 파일인 `billing.sql`을 Git 추적에서 제거하고 GitHub에서 삭제했습니다.
  - `.gitignore`에 `billing.sql`을 추가하여 향후 재커밋을 방지했습니다.
- **DB 접속 설정 파일(`util/config.php`) Git 추적 제거**
  - 데이터베이스 호스트, 계정, 비밀번호가 평문으로 포함된 설정 파일을 Git 추적에서 제거하고 GitHub에서 삭제했습니다.
  - `.gitignore`에 `util/config.php`를 추가하여 향후 재커밋을 방지했습니다.

---

## [2026-06-27]

### 추가 및 개선 사항 (Feat & Refactor)
- **문서파쇄 서비스 페이지 동영상 리소스 최적화 및 갱신 (`shred.php` / `shredding3`)**
  - **대상 영상**: 문서파쇄 페이지에서 새롭게 참조하는 `shredding3.mp4` 파일
  - **대역폭 최적화**: 배경 재생용 영상임을 감안하여 오디오 트랙을 삭제(`-an`)하여 용량 감소.
  - **MP4 용량 감소**: 1280x720 해상도에서 `960x540` 해상도로 조절하고, H.264 코덱 및 CRF 32 설정을 적용하여 파일 크기를 **2.38 MB**에서 **592 KB (약 75% 절감)**로 축소.
  - **WebM 지원 추가**: 최신 모바일 및 웹 환경 최적화 대응을 위해 `shredding3.webm` (VP9, CRF 45, **743 KB**)을 신규 인코딩하여 배치.
  - **체감 로딩 속도 향상 (Poster 및 Faststart)**:
    - 다운로드 완료 전 스트리밍 재생을 위한 MP4 메타데이터 정렬(`-movflags +faststart`) 적용.
    - 영상의 첫 프레임을 고화질 스크린샷(`shredding3_poster.jpg`, **43 KB**)으로 추출하여 포스터 이미지로 지정, 비디오 로딩 전 빈 공간 방지 및 즉각적인 비주얼 노출.
  - 원본 영상은 `shredding3_orig.mp4`로 안전하게 백업.
- **문서파쇄 서비스 페이지 동영상 리소스 최적화 및 갱신 (`shred.php` / `shredding4`)**
  - **대상 영상**: 문서파쇄 페이지에서 새롭게 참조하는 `shredding4.mp4` 파일
  - **대역폭 최적화**: 배경 재생용 영상임을 감안하여 오디오 트랙을 삭제(`-an`)하여 용량 감소.
  - **MP4 용량 감소**: 1280x720 해상도에서 `960x540` 해상도로 조절하고, H.264 코덱 및 CRF 26 설정을 적용하여 파일 크기를 **2.24 MB**에서 **1.16 MB (약 48% 절감)**로 축소.
  - **WebM 지원 추가**: 최신 모바일 및 웹 환경 최적화 대응을 위해 `shredding4.webm` (VP9, CRF 35, **1.34 MB**)을 신규 인코딩하여 배치.
  - **체감 로딩 속도 향상 (Poster 및 Faststart)**:
    - 다운로드 완료 전 스트리밍 재생을 위한 MP4 메타데이터 정렬(`-movflags +faststart`) 적용.
    - 영상의 첫 프레임을 고화질 스크린샷(`shredding4_poster.jpg`, **46 KB**)으로 추출하여 포스터 이미지로 지정, 비디오 로딩 전 빈 공간 방지 및 즉각적인 비주얼 노출.
  - 원본 영상은 `shredding4_orig.mp4`로 안전하게 백업.

---

## [2026-06-26]

### 추가 및 개선 사항 (Feat & Refactor)
- **메인 배경 동영상 최적화 및 교체 (`home_movie5`)**
  - 메인 페이지(`index.php`)의 배경 비디오를 새 리소스인 `home_movie5.mp4`로 업데이트했습니다.
  - **대역폭 최적화**: 무음(Mute) 배경 영상임을 감안해 오디오 트랙을 완전히 삭제하여 파일 크기를 크게 단축했습니다.
  - **MP4 용량 감소**: H.264 코덱 및 적정 품질 수준(CRF 30)을 사용해 용량을 기존 **2.56 MB**에서 **1.34 MB (약 47.7% 절감)**로 축소했습니다.
  - **WebM 지원 추가**: 최신 웹 환경 및 모바일에 최적화된 고압축 WebM 포맷(`home_movie5.webm`, **1.50 MB, 약 41.4% 절감**)을 신규 인코딩해 배치했습니다.
  - **스트리밍 최적화 (Faststart)**: 다운로드가 100% 완료되기 전에 영상이 브라우저에서 즉시 스트리밍 및 재생될 수 있도록 MP4 메타데이터(`moov atom`) 위치를 파일 시작 부분으로 재배치했습니다.
  - 기존 배경 영상인 `home_movie3.mp4` 및 WebM 버전에 대해서도 동일 규격으로 최적화 완료했습니다.
  - 원본 영상 파일들은 접미사 `_bak`를 붙여(`home_movie5_bak.mp4` 등) 안전하게 백업을 구성했습니다.
- **사업분야 동영상 최적화 및 멀티 소스 적용 (`shred.php`, `recycle.php`)**
  - **대상 영상**: 문서파쇄 페이지의 `shredding2.mp4` 및 폐동케이블 재활용 페이지의 `cable2.mp4` 동영상
  - **대역폭 최적화**: 무음 자동 재생 비디오 특성에 맞춰 오디오 트랙을 제거하여 파일 크기를 크게 절감했습니다.
  - **용량 최적화**:
    - `shredding2.mp4` 용량을 기존 **2.70 MB**에서 **1.11 MB (H.264, 약 58% 절감)**로 축소하고 **1.12 MB의 WebM** 버전도 신규 생성하여 최적 배치했습니다.
    - `cable2.mp4` 용량을 기존 **1.61 MB**에서 **597 KB (H.264, 약 64% 절감)**로 축소하고 **495 KB의 WebM** 버전도 신규 생성하여 최적 배치했습니다.
  - **스트리밍 & 체감 로딩 속도 극대화**:
    - 첫 프레임을 추출한 스크린샷 이미지(`shredding2_poster.jpg`, `cable2_poster.jpg`)를 포스터(`poster`) 속성으로 지정하여 즉각적인 로딩 화면을 보여주도록 구성했습니다.
    - MP4 메타데이터 정렬(`-movflags +faststart`)을 마쳐 스트리밍 재생이 끊김 없이 신속하게 시작되도록 개선했습니다.
  - **멀티 소스 지원**: `<video>` 태그 내에 고효율 `.webm` 파일을 우선 탑재하고 하위 호환성을 위해 `.mp4`로 폴백하는 방식으로 태그를 보완했습니다.
  - 원본 영상들은 `_orig` 접미사를 붙여 백업하였습니다.

### 문서 및 프로젝트 관리 (Docs, Refactor & Chore)
- **프로젝트 문서 업데이트 및 이력 관리 체계 구축**
  - 프로젝트 소개 및 실행 환경 정비를 위해 `README.md` 문서를 전반적으로 리뉴얼 및 보강했습니다.
  - 업데이트 및 수정사항 추적을 위해 `CHANGELOG.md`를 도입하고 파일명을 대소문자 표준(`CHANGELOG.md`)에 맞추어 보정했습니다.
- **불필요한 파일 및 데모 리소스 삭제**
  - 개발 과정에서 더 이상 사용되지 않는 테스트용 파일(`demo.php`, `index_1.php`) 및 헬로 월드 성격의 웹 서비스 스크립트(`helloservice.php`)를 제거하여 프로젝트 구조를 경량화했습니다.
- **버전 관리(Git) 제외 및 보안 강화**
  - 로컬 데이터베이스 덤프 스크립트인 `member.sql`을 Git 관리 대상에서 제외하고 `.gitignore`에 등록하여 정보 유출 위험을 방지했습니다.
  - 사용하지 않는 카카오 로그인/연동 예제 파일(`kakao_rest_api_example.php`)과 네이버 도메인 소유권 확인 HTML 파일(`naver*.html`)들을 제외 처리하고 `.gitignore`에 규칙을 추가했습니다.

---

## [2026-06-25]

### 기능 추가 및 개선 (Feat & Refactor)
- **메인 페이지 히어로 영역 비디오 배너 전환**
  - 기존 정적 배너 이미지 노출 방식에서 자동 재생되는 HTML5 비디오 레이어로 교체했습니다.
  - Revolution Slider 모듈 내에서 비디오 로드 로직을 구성했습니다.
  - 사용되지 않는 두 번째 슬라이드 코드를 주석 처리하여 불필요한 스크립트 실행 및 네트워크 부하를 해소했습니다.
- **파쇄 프로세스 페이지 영상 리소스 탑재 (`shred.php`)**
  - 문서 파쇄 시연 과정을 보여주는 전용 비디오 에셋을 추가하고, 페이지 내 재생 로직을 구현했습니다.
- **재활용 페이지 동영상 레이아웃 개선 (`recycle.php`)**
  - 공정 시연 동영상 요소를 추가로 배치했습니다.
  - 설명 문단(`p` 태그)들의 마크업을 구조화하여 페이지 전체 레이아웃의 정렬 상태 및 가독성을 보완했습니다.
- **회사소개 및 위치 레이아웃 조정**
  - 회사소개 페이지(`about-us.php`)의 이미지 스케일과 문단 간 여백(마진/패딩)을 화면 규격에 맞춰 미세 조정했습니다.
  - 지도 영역 안내 팝업(인포윈도우)의 설명 텍스트 디자인과 레이아웃 구조를 깔끔하게 보정했습니다.

---

## [2026-06-24]

### 리소스 수정 (Assets)
- **배너 영역 이미지 교체**
  - 메인/서브 페이지 상단 비주얼 영역 배너용 이미지 파일을 고해상도 리소스로 수정 및 업데이트했습니다.

---

## [2026-06-22]

### 성능 최적화 및 초기화 (Init & Perf)
- **페이지 초기 렌더링 딜레이 제거**
  - 최초 진입 시 무의미하게 발생하던 로딩 스피너 및 지연 모션을 비활성화하여, 사용자가 사이트 콘텐츠에 더 빠르게 접근할 수 있도록 초기 응답 속도를 개선했습니다.
- **배너 및 홈페이지 이미지 리뉴얼**
  - 모바일 해상도 대응을 고려하여 홈 메인 및 배너 이미지들의 품질과 규격을 보정했습니다.
- **조직도 및 레이아웃 수정**
  - 신규 조직도 이미지(`img/organization/organization3.png`)를 추가하고, `organization.php` 및 `price.php` 등의 페이지 구성을 수정했습니다.
  - 공통 영역 레이아웃 개선을 위해 헤더(`include/header.php`) 및 푸터(`include/footer.php`) 마크업을 보완했습니다.
  - 불필요한 로컬 편집기 및 업로드 설정 파일(`*.sublime-project`, `*.sublime-workspace`, `sftp-config.json`)과 임시 파일(`test.php`)을 정리하여 원격 저장소를 깔끔하게 유지하도록 구성했습니다.
- **프로젝트 초기화**
  - Git 버전 관리를 적용하고 메인 브랜치(`main`) 초기 병합 및 리포지토리 환경을 셋업했습니다.

---

## [2024-01-17]

### 기능 추가 및 개선 (Feat & Refactor)
- **콘텐츠 및 소스 코드 수정**
  - `recycle.php`(폐동케이블 재활용) 및 `shred.php`(문서 파쇄) 페이지의 시연 및 설명 콘텐츠를 전면 보완하고 가독성을 위해 문단을 정리했습니다.
  - 회사 소개(`about-us.php`) 및 상단 네비게이션 구조(`include/header-nav.php`)의 텍스트와 링크를 일부 조정했습니다.
- **지도 및 우편번호 조회 API 키 수정**
  - `contact-us.php`에 지정된 지도 검색 및 우편번호 관련 카카오/다음 API의 자바스크립트 키를 수정 및 갱신했습니다.
- **PDF 뷰어 모듈 탑재**
  - 사이트 내 문서 조회를 위해 PDF.js 라이브러리 및 뷰어 리소스(`web/*`)를 신규 구성 및 업로드했습니다.

---

## [2022-02-22]

### 개발 환경 테스트 (Test)
- **테스트 환경 셋업**
  - 환경 동작성 확인을 위해 간단한 `helloworld.py` 스크립트를 추가하여 동작을 검증했습니다.

---

## [2016-12-21]

### 프로젝트 초기화 (Init)
- **초기 저장소 셋업 (Initial Setup)**
  - Git 버전 관리를 시작하고 최초 리포지토리를 구성했습니다.
  - 초기 환영 페이지인 `index.php`를 삭제하고 기본 `README.md` 및 프로젝트 기초 뼈대를 설정했습니다.
